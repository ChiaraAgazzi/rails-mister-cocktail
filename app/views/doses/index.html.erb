<h1>Doses</h1>
<% @doses.each do |dose| %>
  <li><%= link_to dose.description, dosed_path(dose.id) %></li>
  <%= link_to "Delete", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %></li>
<% end %>

